{{#em-form model=this submit_button=false }}
    {{em-input label="Name" property="participantName" placeholder="Enter the name of the event participant" class="required"}}
    {{em-input label="Organization" property="organization" placeholder="Enter name of participant's organization" class="required"}}
    {{em-input label="Gender" property="gender" placeholder="Enter gender of participant either Male, Female, Other" class="required"}}
{{/em-form}}
